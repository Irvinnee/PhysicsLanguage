particle moving
particle still

moving->position = [0, 0, 0]
moving->velocity = [1, 0, 0]

still->position = [0, 1, 0]

law move(particle p):
    p->position = [p->position[0] + p->velocity[0]*$DELTA,p->position[1] + p->velocity[1]*$DELTA,p->position[2] + p->velocity[2]*$DELTA]
    print(moving->position)

<moving & move>

$TIME = 30
$DELTA = 0.1
